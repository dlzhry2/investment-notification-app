# Terraform
The defined infrastructure for the investment notification project.

```
terraform fmt
terraform plan -var="main_user_email=required@email"
terraform apply -var="main_user_email=required@email" -auto-approve
```
